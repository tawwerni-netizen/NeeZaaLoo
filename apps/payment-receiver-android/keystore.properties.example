# Copy to keystore.properties (git-ignored) to sign release builds.
# Keep the .jks file and these passwords OUTSIDE the repository and backed up:
# a lost key means installed phones can never receive an update.
storeFile=../../../secrets/nizalo-receiver-release.jks
storePassword=
keyAlias=nizalo-receiver
keyPassword=
